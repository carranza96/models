syntax = "proto2";

package object_detection.protos;

// Configuration proto for CenterMatcher
message CenterMatcher {

  // Maximum number of assignments per ground truth box
  optional int32 max_assignments = 1 [default = 12];
}